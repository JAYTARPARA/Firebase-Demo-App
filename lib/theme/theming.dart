import 'package:flutter/material.dart';

class Theming extends StatelessWidget {
  final Color darkTextColor = Colors.black;
  final Color lightTextColor = Colors.white;
  final Color dividerColor = Colors.grey;
  final Color scaffoldColor = Colors.grey[800];
  // final Color scaffoldColor = Colors.white38;
  // final Color scaffoldColor = Colors.black87;

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
